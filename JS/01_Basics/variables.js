const userId = 123456;
//assigned once and we cant change the value

let name = "nandish"
var age = 25
let city 

/*
let  is block scoped and we can declare another variable with same name in other block 
so it is better to use let rather than var 
*/

/*
Prefer not to use var
because of issue in block scope and functional scope
*/

console.table([userId,name,age,city])
